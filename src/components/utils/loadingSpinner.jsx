import React from 'react';
import './loadingSpinner.css';

const LoadingSpinner = () => {
  return (
    <div className="spinner">
      {/* You can add any loading animation or message here */}
    </div>
  );
};

export default LoadingSpinner;
